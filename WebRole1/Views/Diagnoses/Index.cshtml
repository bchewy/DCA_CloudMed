@model IEnumerable<WebRole1.Models.Diagnosis>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Consultation.Status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Illness)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Consultation.Status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Illness)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DiagnosisID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DiagnosisID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DiagnosisID })
        </td>
    </tr>
}

</table>
